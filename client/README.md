# AgentPulse Client

## Run the project

Open two terminals from the project root.

### Backend
```bash
cd backend
npm install
npm start
```

### Frontend
```bash
cd client
npm install
npm run dev
```

Open http://localhost:5173

The Vite development server proxies `/api` requests to the backend at `http://localhost:3000`.
